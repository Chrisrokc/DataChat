# Windows installer assets

Drop optional branding files here:

- `datachat.ico` — installer + shortcut icon (Windows .ico, multi-resolution recommended).
  When present, uncomment the `SetupIconFile=` line in `DataChat.iss`.

The installer compiles fine without an icon — Inno Setup falls back to its default.
